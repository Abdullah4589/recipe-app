# Dependencies
node_modules/
backend/node_modules/

# Backend secrets — never commit
backend/.env

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native build outputs
android/
ios/

# Environment variables — contains API keys, never commit
.env
.env.local
.env.*.local

# macOS
.DS_Store

# Windows
Thumbs.db
Desktop.ini

# Editor directories
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Lock files (pick one to commit — keep package-lock.json if using npm)
yarn.lock

# TypeScript
*.tsbuildinfo

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Certificates & signing keys
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Scratch prompt drafts
meal-planner-prompt_1.md
meal-planner-prompt_2.md
meal-planner-prompt.md

# Expo web export (rebuilt by scripts/serve-web.js for e2e)
dist/

# scripts/test-loop.js working files
loop-report.json
.loop-state.json

# CLAUDE.md and .claude/ are deliberately committed — the agent rules and
# guardrails are part of the project, not personal local config.
.claude/settings.local.json
